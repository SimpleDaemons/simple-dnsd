#!/bin/bash
# Post-removal script for simple-dnsd

set -e

PROJECT_NAME="simple-dnsd"
CONFIG_DIR="/etc/$simple-dnsd"
LOG_DIR="/var/log/$simple-dnsd"

if [ "$1" = "purge" ]; then
    # Remove configuration files (optional - user may want to keep them)
    # Uncomment to remove config on purge:
    # rm -rf "$CONFIG_DIR"
    
    # Remove log files
    rm -rf "$LOG_DIR"
    
    # Remove service user (optional)
    # userdel dnsddev 2>/dev/null || true
fi

exit 0

